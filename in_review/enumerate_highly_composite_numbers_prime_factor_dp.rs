use crate::enumerate_prime_factor_candidates_for_highly_composite_numbers::*;

/// return list of (n, d(n), factorization histogram)
// pub fn enumerate_hcn(less_than: u64) -> Vec<(u64, u16, Vec<usize>)> {
//     let mut hcn
// }
#[cfg(test)]

mod tests {

    #[test]

    fn test() {}
}
